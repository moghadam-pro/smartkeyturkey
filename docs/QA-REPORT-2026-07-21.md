# Production QA report — 21 July 2026

## Automated scores

Mobile Lighthouse run against the live homepage after SmartKey Core `1.0.1`:

| Category | Score |
| --- | ---: |
| Performance | 81 |
| Accessibility | 100 |
| Best Practices | 100 |
| SEO | 100 |

Key homepage measurements were FCP 3.0 s, LCP 3.5 s, TBT 160 ms and CLS 0.02. These are laboratory measurements from one run, not field Core Web Vitals.

The tested single product scored 100 for Accessibility and SEO. The tested Istanbul property page scored 100 for Accessibility after the dark-mode submit-button contrast fix.

## Functional checks

- Homepage, property archive, product archive and blog returned HTTP 200.
- Homepage had one H1, a valid canonical, index/follow robots output and no horizontal overflow at 390 px.
- The Rank Math sitemap index returned HTTP 200 and was declared in `robots.txt`.
- Product RFQ prefilled the current product, required consent and accepted a clearly labeled QA submission.
- Contact Form 7 displayed its sent confirmation and SmartKey Core recorded one request against the selected product.
- The first-party property inquiry form required name, valid email and consent and returned its successful sent state after a labeled QA submission.

## Remaining checks

- Re-run Lighthouse after the next content/image optimization batch.
- Confirm Site Kit records the intended form conversion events in GA4.
- Improve initial server response time through CloudPanel/Cloudflare full-page caching after authenticated and form exclusions are defined.
- Consider locally hosted approved font files to remove the external render-blocking font request.
- Run keyboard-only and screen-reader manual checks on the main journeys.

## SmartKey Core 1.1.0 functional addendum

- Verified `SmartKey → Requests` stores both request types and is restricted to administrators.
- Submitted one labeled petrochemical RFQ and one labeled property inquiry; both appeared in the production request list and neither SmartKey-managed flow invokes email delivery.
- Verified the product category sidebar is sticky at desktop and 390 px widths with zero document overflow.
- Verified request inputs use uniform 48 px heights and 8 px radii; submit actions are compact, borderless and right aligned.
- Verified the About Us page title, navigation links, address, map link and contact phone.
- Verified the custom 404 template renders the expected content and viewport-aware minimum height.

## SmartKey Core 1.2.1 attractions and analytics addendum

- Verified the attraction archive contains four guides, one H1, the intended SEO title/description and no horizontal overflow.
- Verified the archive at 390 px: sticky 105 px city filter, single-column results and zero document overflow.
- Verified the Hagia Sophia single guide exposes five visitor facts, official-source attribution and a Google Maps coordinate link.
- Verified the public header contains Attractions and the WordPress SmartKey menu contains Attractions and Attraction Cities.
- Verified `skt_attraction-sitemap.xml` returns HTTP 200 and lists all four pilot guides.
- Verified Site Kit reports Analytics as connected and the deployed frontend contains both first-party conversion event handlers.
- GA4 DebugView/key-event confirmation remains pending because the Site Kit interface does not expose GA4 event administration.

## SmartKey Core 1.3.0 editorial addendum

- Verified the blog archive renders four unique published articles, one H1, three managed category filters and the intended Rank Math title/description.
- Verified the new TURKSTAT article contains the official source link, reviewed date, pending-human-review status, disclaimer and specific meta description.
- Verified desktop and 390 px mobile layouts with zero document overflow; category chips intentionally scroll horizontally on narrow screens.
- Verified `post-sitemap.xml` returns HTTP 200 and includes both newly published URLs.
- Detected and moved one migration-created duplicate to WordPress Trash; the public archive now contains exactly one June 2026 market-update card.
- Added a five-minute migration lock and direct slug lookup to prevent concurrent duplicate creation.
